spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

    defer-datasource-initialization: true

  datasource: # db정보추가
    url: jdbc:h2:mem:testdb
  h2: # H2 콘솔 활성화
    console:
      enabled: true

